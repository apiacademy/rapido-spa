import {
  wizardTest
} from 'rapido/helpers/wizard-test';

module('WizardTestHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = wizardTest(42);
  ok(result);
});
